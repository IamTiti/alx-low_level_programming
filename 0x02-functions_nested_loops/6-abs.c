#include "main.h"
/**
 * _abs - computes absolute value of an integer
 *
 * @n: int is the variable for the program.
 * Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);

}
